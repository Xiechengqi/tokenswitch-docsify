# （已下线）附加设置

> **本页已下线 · 只读 · 不代表当前实现**
>
> 保留此页只是为了让旧链接不 404。内容不再维护。

旧的 Share 附加设置（`accessByApp`、`appSettings`、`officialPricePercent` 等）已全部退役，出现即拒绝。当前 Share 只有 `freeAccess` + `userGrants` + `tokenLimit` / `parallelLimit`。见 **[创建 Share](/provider/share)**。

完整的前后对照见 **[已下线功能](/reference/retired)**。

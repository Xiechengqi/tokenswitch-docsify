# （已下线）市场 API Key

> **本页已下线 · 只读 · 不代表当前实现**
>
> 保留此页只是为了让旧链接不 404。内容不再维护。

市场签发的 `sk-...` key 已全部失效。现在用 Router 用户 API Token（`/account/api-keys`，scope `share:invoke`），一个 Token 走遍你有权访问的所有 Share。见 **[在 CLI 中使用](/share-market/using-cli)**。

完整的前后对照见 **[已下线功能](/reference/retired)**。

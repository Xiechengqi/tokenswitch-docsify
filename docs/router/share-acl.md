# （已下线）Share ACL

> **本页已下线 · 只读 · 不代表当前实现**
>
> 保留此页只是为了让旧链接不 404。内容不再维护。

`acl` 字段已退役，出现即拒绝（camelCase 与 snake_case 双向 fail-closed）。授权用户、来源和个人配额的唯一真值是 `userGrants`。见 **[创建 Share](/provider/share)** 和 **[市场准入与授信](/provider/access)**。

完整的前后对照见 **[已下线功能](/reference/retired)**。
